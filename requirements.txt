# Python dependencies for max_llm
# Note: torch & torch-dependent packages (xformers, deepspeed, bitsandbytes) are
# installed separately in setup.sh Step 4 after torch is available with CUDA 12.1.

# Base
pydantic>=2.0.0

# Core model
numpy>=1.24.0

# Training
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
wandb>=0.16.0
huggingface-hub>=0.19.0
fire>=0.5.0

# Tokenization
sentencepiece>=0.1.99
tokenizers>=0.14.0

# Monitoring
tensorboard>=2.14.0
tensorboard-plugin-profile>=2.14.0
tqdm>=4.66.0

# Development
black[jupyter]>=23.12.0
ruff>=0.1.11
isort>=5.13.0
mypy>=1.7.0
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
pre-commit>=3.5.0
